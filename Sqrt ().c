#include <stdio.h>
#include <math.h>

int main ()

{
	int number;
	
	printf("Enter The Number : ");
	scanf("%d", &number);
	
	printf("Square Root = %lf", sqrt (number) );
	
	return 0;
}
